# Production — talks to the live backend.
# Used by `next build` / `next start`. The /api suffix is required because the
# axios baseURL is this value verbatim and routes are mounted under /api/*.
NEXT_PUBLIC_API_URL=https://sail-miom.ddns.net/api
